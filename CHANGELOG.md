# 更新记录

## v0.1.0 — 2026-09-21　街区版

第一个完整可用的版本：一个 600m × 440m 的示例城区，从标记图到三维场景全流程打通。

### 工具链
- `tools/map2scene.py`：色块标记图 → `scene.json`
  - 建筑轮廓直角化（主方向吸附、抹平台阶、斜边保留）
  - 道路骨架 → 中心线 / 路口 / 路宽；路口节点合并与归正（各条路在路口外的直线段最小二乘求交）
  - 斑马线（距离按相交道路半宽）、店门、出入口自动生成
  - 面状区域：公园、绿化带、水体、广场、停车场
  - 环岛自动识别（路面里又小又圆的洞）；高架（单独一层，补回被它盖住的地面路，标出桥下道路）
  - 低饱和度像素一律视为未标记，灰色底图不干扰
- `tools/sat2marks.py`：卫星图半自动标注（本地网页）
  - SAM 点选（CUDA → sam2.1_l；CPU → mobile_sam；没装 torch → 漫水填充）、画路、多边形、自动植被、标尺、撤销、断点续标
  - 输出与 map2scene 完全兼容的透明标记图层
- `tools/make_demo_map.py`：生成示例标记图

### 运行时（Vue 3 + Three.js，`src/city/`）
- 程序化建筑（圆角墙体、挑檐女儿墙、临街店面、屋顶设备），雾化背景楼块，行道树 / 绿地树
- 人群：导航网格 + 每个目的地一张距离场；逛店、歇脚、等红灯；屋顶热力图
- 红绿灯：两相位、行人过街窗口、停车线
- 车流：按路宽划分 1~4 车道、导向车道、红灯右转、转弯让行人 / 让直行（进路口前做轨迹冲突检查）、
  环岛让行、超车并驶回原车道、路中遇行人让行
- 高架：桥面 / 护栏 / 桥墩、桥下隔离带、自动生成的上下匝道（匝道岛、护栏缺口）
- 停车场（倒车出位、沿通道进出）与地下车库（面积够大且临路的楼自动配）
- 室内视图：点击建筑 → 商场一层 / 地下车库 B1，人数和停车数与仿真实时联动
- 视角：鼠标 / 键盘 / 触屏 / 屏幕按钮 / 复位

### 实测
- 600 行人 + 约 270 辆车（含停着的），约 50 个 draw call，集显笔记本 60fps
- 车流连续运行 400s+ 不堵死；车辆中心距 < 2m 的采样 4/1200
- 通行规则参照《道路交通安全法》第 47 条、《实施条例》第 38 / 51 / 52 条（示意性实现，不是交通仿真）

### 已知限制
- 车不为转向变道（进路段时选好导向车道）；高架只支持整体架空、两端出图
- 室内是按轮廓程序化的示意布局；SAM 分出的是屋顶不是墙脚，高层建筑需要人工挪轮廓
- GPU 上的 SAM 大模型分支未在本机验证

## v0.2.0 — 2026-09-22　城区版

2.4km × 1.7km 的城区示例：统一仿真时钟与日历、轨道交通、人群分组与场馆活动、住户出行、车站 / 路灯 / 多样化建筑造型；
全部源码注释率 ≥ 50%，vitest 145 例 + pytest 27 例。

### 造型
- 车站（`stations.js`）：地铁出入口亭 + 站名柱、高铁站房（弧形大屋顶、站台、站前广场）、地铁 + 铁路综合枢纽（两翼换乘厅）；
  站前广场自动选压到建筑更少的一侧
- 路灯（`lamps.js`）：主干路双臂高杆灯、普通路单臂灯、高架护栏灯、公园 / 广场 / 住宅区庭院灯；夜里点亮并投光斑
- 建筑（`buildings.js`）：住宅（板楼 / 退台 / 点式）、写字楼（幕墙 / 格窗裙房 / 收分）、商场与小商铺各有原型，按编号确定性选择

### 注释与测试
- 所有 `src/city`、`src/components`、`tools` 文件注释率 ≥ 50%（`python tools/comment_ratio.py src/city src/components tools`，合计 53%）
- vitest（`npm test`）：16 个文件 145 例，覆盖时钟、几何、车道、导航网格、红绿灯、需求、人群、车流、轨道、建筑、地面、室内、路灯、车站、配景
- pytest（`npm run test:py`，`tests/py/`）：27 例，覆盖 map2scene 的归类 / 轮廓 / 直角化 / 骨架图 / 折线工具，sat2marks 的漫水填充 / 掩膜整理 / 自动植被 / 会话编辑与续标，示例地图工具
- 无 canvas 环境（node 测试）下屋面 / 铺装贴图返回 null，浏览器行为不变

### 时间
- `clock.js`：全城唯一的仿真时钟 + 日历（工作日 / 周末 / 节假日，含调休配置）。仿真速度 1×~120×，
  人走路、车行驶、红绿灯、停留时长、发车间隔全部按同一个仿真时间推进；引擎每帧切成小步保证高倍速下物理稳定
- 昼夜光照、夜里亮窗；手动跳时间后按新时刻重新布置人群和列车

### 城区示例（`?scene=district`，2.4km × 1.7km）
- `tools/make_district_map.py`：格网路网、环形高架 + 横贯快速路、中央嵌入街区示例、体育场 / 大剧院、
  地块按类型自动填充、斜向铁路绿廊；标记图上画不了的东西（轨道交通、场馆信息、核心区范围）放在 sidecar json
- `map2scene.py --sidecar`；新增住宅、活动场馆两种建筑色；桥下道路的桥面半宽取沿线中位数
- 前端：导航网格只铺核心区、距离场按楼建（多源）、路口检查只看本路口的车、阴影相机跟随视野

### 轨道交通（`transit.js`）
- 三条地铁（南北 / 东西 / 环线）用透视叠加显示线路、站点、列车；一条斜向弯曲的高架铁路（实体桥）
- 工作日 / 节假日两套时刻表，发车间隔随时段变化，夜间停运；同名车站为换乘站
- 核心区内的车站有地面出入口：列车到站成批放人，人群也从车站离开

### 人群与活动（`demand.js`）
- 上班族 / 老年人 / 青年 / 访客：各自的出现曲线（工作日、休息日）和分时段活动偏好；
  每结束一段停留按「人群 × 时段」选下一站，办公楼待到午饭点 / 下班点，小人按人群配色
- 场馆活动按日历确定性排期（体育场周末比赛、剧院晚场 / 日场）：进场时段吸引力随进度增长，观众待到散场后离开

### 场馆造型与住户出行
- 体育场：阶梯看台 + 草坪 + 环形罩棚；剧院：基座 + 椭球壳体 + 玻璃幕带（只依赖轮廓和 `venue.type`）
- 住户出行：每栋住宅楼有住户数（建筑面积 / 45㎡ × `residentScale`），按时段的离家率出门 ——
  工作日早上以上班族为主、多数去车站通勤离开核心区，其余在本地活动；本地活动的住户「离开」= 回家；
  在外的住户傍晚从车站 / 出入口回来走回自己那栋楼。在家 + 活动 + 外出 三个数全天守恒
- 实测（核心区 10 栋住宅 241 位住户，工作日）：6:00 在家 231 → 7:22 在家 131 / 本地 46 / 外出 64；
  17:30 外出 141 → 18:23 外出 81 → 21:20 在家 208

### 实测（集显笔记本）
- 城区：270 栋楼、778 条车道、110 个信号路口、20 条匝道、约 360 辆在途车、早高峰 18 列地铁；160 万三角面 / 约 40 draw call
- 120 倍速每帧 CPU 约 8ms；周六足球赛：进场 60 分钟体育场 205→824/959 人，散场 20 分钟清空，总人数始终跟随应有人数

### 已知限制
- 核心区（约 890m × 670m）之外不逐人仿真；活动观众按 0.15 的比例缩小（`demandOptions.eventScale`）
- 人是「需求驱动」而非持久个体：没有一个人从早到晚的完整行程（住户只保证「从哪栋楼出来就回哪栋楼」和人数守恒）
- 地铁线路不检查与建筑的空间关系（地下）；铁路桥墩可能落在路面上；场馆造型只有体育场和「壳体」两种
- 节假日表只内置了确定放假的日期，调休需按当年国务院通知补全
